<div class="page">
  <div class="page-left">
    <a href="#explore-area" class="green-button mobile-area-toggle mobile-only">Explore</a>
    
    <div id="explore-area" class="mobile-hidden">
      <div class="green-header">
        <h2>Explore</h2>
        <span class="arrow"> </span>
      </div>
  
      <ul class="side-nav">
        <% @body_parts.each do |body_part| %>
          <% if @body_part.name == body_part.name %>
            <li><%= link_to body_part.name, body_part_path(@body_part), :class => :current %>
              <ul>
                <% @body_part_sections.each do |body_part_section| %>
                  <% if @body_part_section.name == body_part_section.name %>
                    <li><%= link_to body_part_section.name, body_part_body_part_section_path(@body_part, body_part_section), :class => :current %></li>
                  <% else %>
                    <li><%= link_to body_part_section.name, body_part_body_part_section_path(@body_part, body_part_section) %></li>
                  <% end %>
                <% end %>
              </ul>
            </li>
          <% else %>
            <li><%= link_to body_part.name, body_part_path(body_part) %></li>
          <% end %>
        <% end %>
      </ul>
    </div>
  </div>

  <div class="page-middle">
    <div class="middle-inner">
      <div class="know-your-body-teaser green-header">
        <h3>This is only a small taste of what we offer...</h3>
        <span class="arrow"></span>
        <p>If you are a patient of Physio Med you will automatically have access to our full 
        <strong>Know Your Body</strong> section where you will find a vast resource on the types of <%= @body_part.name.downcase %> injuries 
        that you could suffer from as well as information about the steps that should be taken 
        for fixing this problem.</p>
      </div>
      <h1><% title "#{@body_part.name} : #{@body_part_section.name}" %></h1>

      <h1><%= @body_part.name %></h1>

      <h2 class="subtitle"><%= @body_part_section.name %></h2>

      <%= raw @body_part_section.content %>

      Portions of this document copyright MMG, LLC.
      
      <hr />
      
      <%= render "shared/social_links" %>
    </div>
  </div>
  <div class="page-right">
    <% unless @body_part.image.blank? %>
      <%= image_tag @body_part.image.show, :alt => @body_part.name, :class => 'body-part-image' %>
    <% end %>
    
    <%= render "shared/no_visual_right_side_contact" %>
    
    
    <% unless @testimonials.blank? %>
      <div class="quote">
        <% @testimonials.each do |testimonial| %>
        <span class="open"></span>

          <p><%= testimonial.content %></p>

        <span class="close"></span>
        <% end %>
      </div>
    <% end %>
    
  </div>
  
  <div class="clear"></div>
</div>

